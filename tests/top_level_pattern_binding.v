(** File generated by rocq-of-ocaml *)
Require Import RocqOfOCaml.RocqOfOCaml.
Require Import RocqOfOCaml.Settings.

Definition _left : int :=
  let '(_left, _right) := (1, "two") in
  _left.

Definition _right : string :=
  let '(_left, _right) := (1, "two") in
  _right.

Definition nested : string :=
  let '((nested, flag), count) := (("value", true), 3) in
  nested.

Definition flag : bool :=
  let '((nested, flag), count) := (("value", true), 3) in
  flag.

Definition count : int :=
  let '((nested, flag), count) := (("value", true), 3) in
  count.
