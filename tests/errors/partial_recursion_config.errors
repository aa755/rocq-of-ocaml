--- tests/errors/partial_recursion_config.ml:2:11 -------------------------- not_supported (1/1) ---

  1 | let consume bound =
> 2 |   let rec loop value =
  3 |     if value < bound then loop (value + 1) else value
  4 |   in
  5 |   loop 0


@rocq.partial requires Delay effect propagation through callers, which is not implemented yet.

--- Errors ---------------------------------------------------------------------------[ 1 error ]---

❌ File '/dev/stdout' generated with some errors
(** File generated by rocq-of-ocaml *)
Require Import RocqOfOCaml.RocqOfOCaml.
Require Import RocqOfOCaml.Settings.

Definition consume (bound : int) : int :=
  let fix loop (value : int) : int :=
    if RocqOfOCaml.Basics.Stdlib.lt value bound then
      loop (Z.add value 1)
    else
      value in
  loop 0.
